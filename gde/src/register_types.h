#pragma once

#include <godot_cpp/core/class_db.hpp>

using namespace godot;

void initialize_growth_sim_module(ModuleInitializationLevel p_level);
void uninitialize_growth_sim_module(ModuleInitializationLevel p_level);
